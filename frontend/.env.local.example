# Leave empty for Vercel (uses rewrites). Set to http://localhost:8000 for local dev.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Server-side only: backend URL for Next.js rewrites (set on Vercel)
API_BACKEND_URL=http://localhost:8000
