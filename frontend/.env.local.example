# Backend API URL for client-side fetch (browser → backend directly)
# For local dev: http://localhost:8000
# For Vercel: leave empty (uses rewrites via API_BACKEND_URL below)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Server-side only: backend URL for Next.js rewrites (proxy /api/* → backend)
# For Vercel: set to your Railway backend URL, e.g. https://thesis-engine-production.up.railway.app
API_BACKEND_URL=http://localhost:8000
