[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "alembic upgrade head && python -m scripts.seed_companies && uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8080}"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
